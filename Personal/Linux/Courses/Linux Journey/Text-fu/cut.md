The `cut` command allows extraction of text from files containing text. It is possible to do this using delimiters and fields

For example this extract the first field separated by a ';'
```bash
cut -f 1 -d ";" sample.txt
```

