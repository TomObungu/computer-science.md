The `>` operator effectively redirects the output of command away from `stdin`.

When you run a command, the output is `stdin` by default which is the terminal screen.

Using `>` can be used to say instead output the result of a command inside a file

