The `#!/bin/bash` is called a `shebang` line. It tells the system which interpreter should be used to run a shell script. In this case is the `/bin/bash` variable

You can add permissions using `chmod`