You can use the redirection operator `>` to create new files. This is a quick way to write text into a file directly from your terminal. Press `ctrl+D` or `ctrl+C` to save an exit.

To append to a file isntead of overwriting it use `>>`

Use `cat` for short files. For long files use `less` so you can scroll, search and quit